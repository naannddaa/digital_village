<?xml version="1.0" encoding="utf-8"?>
<layer-list android:shape="rectangle" xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape android:shape="rectangle">
            <solid android:evalation="#80000000" /> <!-- 50% transparan hitam -->
            <corners android:radius="8dp" /> <!-- Radius sudut untuk shadow -->
            <padding
                android:left="5dp"
                android:top="5dp"
                android:right="5dp"
                android:bottom="5dp" /> <!-- Offset shadow -->
        </shape>
    </item>

    <!-- Layer untuk background EditText -->
    <item android:left="0dp" android:top="0dp" android:right="0dp" android:bottom="0dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Warna latar belakang -->
            <corners android:radius="8dp" /> <!-- Radius sudut latar belakang -->
        </shape>
    </item><corners android:radius="25dp"/>
    <solid android:color="@color/white"/>
        <size android:height="50dp"/>
</layer-list>